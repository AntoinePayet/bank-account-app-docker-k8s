apiVersion: v1
kind: Service
metadata:
  name: {{ include "angular-front-end.fullname" . }}
  labels:
    {{- include "angular-front-end.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "angular-front-end.selectorLabels" . | nindent 4 }}
